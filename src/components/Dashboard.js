import React, { Component } from 'react';
import { connect } from 'react-redux';

import ServiceWidget from './Shared/ServiceWidget';

class Dashboard extends Component {
  render() {
    return(
      <div>
        Dashboard!
      </div>
    );
  }
}

export default Dashboard;